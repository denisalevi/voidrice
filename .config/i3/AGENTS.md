This folder contains my i3 configuration.

`config` is the main i3 config.
`i3blocks.conf` configures the i3blocks status bar.
`config` uses `status_command i3blocks -c ~/.config/i3/i3blocks.conf` for the bar.
`keybindings` contains keybinding definitions included by `config`.
`scripts/` contains i3blocks helper scripts (battery, volume, cpu, etc).

I use i3blocks as status bar, please read the docs here if suggesting anything about i3blocks: https://vivien.github.io/i3blocks/

My dotfiles use a custom git wrapper command called `dotfiles` (use `dotfiles status .` in this folder). Check status before you modify anything and tell me if there are uncommited or untracked changes; I will handle committing them. After you made changes, ask if you should commit and push the changes and suggest a commit message; wait for my approval.

When suggesting a commit message, always include a note/trailer that the changes were generated by you and include the model name (`GPT-5.2`), optionally as a `Co-authored-by:` trailer.

When you find out more info about the structure of this folder, please update this AGENTS.md, though keep it concise please. You may remove information here with updated content.
